---
import { getIndex } from "@lib/contentParser";
import EntryLayout from "@components/facts/EntryLayout.astro";
import type { IndexCardsEntry } from "@/types";

const entry = (await getIndex("indexCards")) as IndexCardsEntry;
---

<EntryLayout entry={entry}/>
